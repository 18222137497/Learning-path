package com.tarena.yadx.circulation;

public class TextCopy {
	/*
	 * 一，数组的复制
	 *  1.System.arraycopy()方法
	 *  -格式
	 *  public static void arraycopy(Object src,//原数组
	 *                               int arcPos,//元素组的起始位置
	 *                               Object dest,//目标数组
	 *                               int destPos,//目标数组的起始位置
	 *                               int length)//要复制的数组元素数量
	 *  2.Array.copyOf方法
	 *  -使用java.util.Array类的copyOf方法实现数组的复制
	 *   -格式
	 *      类型[] newArray=Arrays.copyOf(类型[] src,int newlength);
	 *   -特点，生成的新数组是原始数组的副本
	 *    newlength小于，截取
	 *    newlength大于，扩容                                   
	 */
	
	
	

}
