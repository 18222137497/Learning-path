package com.tarena.yadx.arr;

public class TestArr {
	/*
	 * 一.数组是对象
	 *   1.在java数组属于引用数据类型
	 *   2.数组对象存储在堆中，数组变量（引用）属于引用类型，存储数组对象的地址信息
	 *    指向数组对象 
	 *   3.数组的元素可以看成对象的成员变量，只不过类型全部相同 
	 * 二.引用类型数组的声明
	 *   1.数组元素可以是任何类型，当然也包括引用类型数组
	 * 三.应用类型数组的初始化
	 *   1.引用类型数组的默认初始值都为null
	 *   2.如果希望每一个元素都指向具体的对象需要针对每一个对象每一个数组元素进行new运算
	 *   例如：
	 *     Person p=new Person[]{new Person() ,new Person(),new Person()}
	 * 四.数组的元素可以为任意类型，也包括数组类型
	 *    int[] arr=new int[3][];
	 *    arr[0]=new int[2];
	 *    arr[1]=new int[3];
	 *    arr[1][1]=100;
	 *            
	 */

}
