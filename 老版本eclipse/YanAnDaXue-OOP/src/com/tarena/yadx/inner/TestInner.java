package com.tarena.yadx.inner;
/**
 * 内部类
 * @author 123
 *
 */
public class TestInner {
	/*
	 * 一、定义成员内部类
	 *   -一个类可以定义在另外一个类的内部，定义在内部的类叫做Inner，定义在外部类叫做Outer
	 *   -Inner定义在Outer内部，通常只服务于Outer，对外不具有可见性，Inner可以直接调用Outer的成员变量及方法（包括私有的）
	 * 二、创建内部类对象
	 *   -一般情况下Inner对象会在Outer对象中创建(构造方法或其他方法)；Inner对象中会有一个隐式的引用指向创建他的Outer类对象
	 * 三、定义匿名内部类
	 *   -如果在一段程序中需要创建一个类的对象（通常需要实现某个接口或者继承某个类），而且对象创建后这个类的价值也就不存在了
	 *    这个类不必命名，称为匿名内部类    
	 */

}
