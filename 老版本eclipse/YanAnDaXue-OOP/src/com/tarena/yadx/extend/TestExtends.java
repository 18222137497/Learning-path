package com.tarena.yadx.extend;
/**
 * 继承的意义
 * @author 123
 *
 */
public class TestExtends {
	/*
	 * 一、泛化的过程
	 *   泛化在当前继承中指：将具备共同属性和方法的类，泛化为一个具有相同属性和方法的父类。
	 *   子类通过继承父类的形式获取到父类的属性及方法。
	 * 二、extends关键字
	 *   1.通过extend关键字可以实现类的继承。
	 *   2.子类（Sub class）-基类 可以继承父类(Super class)-超类的成员变量和成员方法，同
	 *     时也可以定义子类所特有的成员变量和方法。
	 *   3.java语言不支持多继承，一个子类只能继承一个父类（一个爸爸能有好多儿子，一个儿子
	 *     只能有一个爸爸）
	 * 四、父类的引用指向子类对象---多态
	 *     Person p=new person();
	 *     Father f=new Father();
	 *     Son s=new Son();
	 *     Father f=new Son();----多态
	 *   1.一个子类的对象可以向上造型为一个父类的类型。即定义父类的引用指向子类的对象
	 *   2.当使用向上造型后，用父类的引用调用子类扩展的属性方法则会出现编译错误       
	 *         
	 */
	public static void main(String[] args){
		
		
	
		
	}

}
