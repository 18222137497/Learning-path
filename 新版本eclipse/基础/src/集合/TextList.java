package 集合;
/**
 * List集合三大特点：
 * 	1.有序的collection（存储与取出的顺序是一致的）
 * 	2.集合有索引
 * 	3.与set不同，List允许存储重复的元素
 * 带索引的方法（特有的方法）:
 * 	public void add(int index,E element);
 *  public E remove(int index);
 *  public E get(int index);
 *  public E set(int index,E element);//用指定元素替换指定位置的元素
 * @author 123
 *
 */
public class TextList {
	
}
