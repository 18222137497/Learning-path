server:
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis1?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

mybatis:
#指定配置文件包
  #mapper-locations: classpath:mybatis/mapper/*.xml
#配置别名包
  #type-aliases-package:com.wang.pojo
#指定主配置文件位置
  #config-location:classpath:mybatis/mybatisConfig.xml